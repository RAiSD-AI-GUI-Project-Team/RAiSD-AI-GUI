# Developer reference

## RAiSD-AI
uses raisd-ai -> see repo and paper

source files for raisd-ai are in sources/

## Install and run
installation is the same as for normal users, see repo#install

when working on the gui make sure to activate the raisd-ai-gui environment so that the testing and running will work

## Configuration
config file, changing it will change the gui

### config file language:
consists of 3 parts...
...

## development
all gui related code is in the gui/ folder

the environment is in ./environment-gui.yml

The gui code is divided into x packages:
### for each package small explanation of function
...

## Testing

run pytest ..

for code coverage run pytest --cov..